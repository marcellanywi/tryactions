name: C/C++ CI

on: [push]

jobs:
  build:

    #runs-on: ubuntu-latest
    runs-on: [linux, arm32]
    steps:
    - uses: actions/checkout@v1
      #- name: configure
      #run: ./configure
    - name: make
      run: make
      #- name: make check
      #run: make check
      #- name: make distcheck
      #run: make distcheck
